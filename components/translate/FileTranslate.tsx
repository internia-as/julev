"use client";
import React from "react";

interface Props {
  langFrom?: string;
  langTo?: string;
}

const FileTranslate = (props: Props) => {
  return <div>File</div>;
};

export default FileTranslate;
